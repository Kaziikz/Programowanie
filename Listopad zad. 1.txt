#include <iostream>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <string>

using namespace std;

int main()
{
	int zadanie = 1;
	while (zadanie != 0)
	{
		switch (zadanie)
		{
			//1.1. Zadeklarowaæ zmienne typu int, float, char. Przypisaæ do nich wartoœæ, wyœwietliæ je na ekran.
		case 1:
		{
			int x;
			float y;
			char litera;

			cout << "Wyswietle wartosci zmiennych" << endl;

			x = 1, y = 2.5, litera = 'z';

			cout << x << endl;
			cout << y << endl;
			cout << litera << endl;

			cout << endl;
		}
		//1.2. Wyœwietliæ zmienn¹ typu int na ekran. Nale¿y u¿yæ metody printf i konstrukcji %d
		case 2:
		{
			cout << "Wyswietlam zmienna int na ekranie" << endl;
			printf("%d", 7);

			cout << endl;
			cout << endl;
		}
		//1.3.Pobraæ od u¿ytkownika liczbê x.Wyœwietliæ kwadrat liczby x.
		case 3:
		{
			int x;

			x = 4;

			cout << "x = " << x << endl;

			cout << "Kwardat liczby x to" << " " << x * x;

			cout << endl;
			cout << endl;
		}
		//1.4.Zapoznaæ siê z instrukcjami warunkowymi(if, else if, else oraz switch).Pobraæ od u¿ywkownika liczbê i wyœwietliæ informacjê, czy jest ona : mniejsza, wiêksza, czy równa 0.
		case 4:
		{
			int liczba;
			liczba = 5;

			if (liczba < 0)
			{
				cout << liczba << " " << "jest mniejsze od 0" << endl;
			}
			else if (liczba == 0)
			{
				cout << liczba << " " << "jest rowna 0" << endl;
			}
			else if (liczba > 0)
			{
				cout << liczba << " " << "jest wieksza od 0" << endl;
			}
			cout << endl;
		}
		// Instrukcja switch
		case 5:
		{
			int banan;
			banan = 3;
			switch (banan)
			{
			case 0:
				cout << "Nie ma bananów" << endl;
			case 1:
				cout << "Masz 1 banana" << endl;
			case 2:
				cout << "Masz 2 banany" << endl;
			case 3:
				cout << "Masz 3 banany" << endl;
				cout << endl;
			}
		}
		//1.5.Pobraæ ud u¿ytkownika 3 liczby(x1, x2, x3).Wyœwietliæ informacjê, która z tych liczb jest najwiêksza.
		case 6:
		{
			cout << "Ktora liczba jest najwieksza" << endl;
			cout << "Wprowadz liczby" << endl;
			cout << endl;
			int x, y, z;
			for (int i = 0; i <= 2; i++)
			{
				cin >> x >> y >> z;
				cout << endl;
				if (x > z, x > y)
				{
					cout << x << " " << "jest najwieksza" << endl;
					break;
				}
				if (y > x, y > z)
				{
					cout << y << " " << "jest najwieksza" << endl;
					break;
				}

				if (z > x, z > y)
				{
					cout << z << " " << "jest najwieksza" << endl << endl;
					break;
				}
				break;
			}
		}
		//1.6. Napisaæ prosty kalkulator. Nale¿y pobraæ od u¿ytkownika 2 liczby oraz operator (jeden z czterech: mno¿enie, dzielenie, dodawanie,
		//	odejmowanie).Wymagane jest utworzenie prostego menu u¿ytkownika.Uwaga - nale¿y pamiêtaæ o u³amkach(1 / 3 powinno daæ 0.33, a nie
		//	0)
		case 7:
		{
			double a, b;
			string znak;
			cout << "Kalkulator" << endl << endl;
			cout << "Podaj pierwsza wartosc" << endl;
			cin >> a;
			cout << "Podaj dzialanie" << endl;
			cin >> znak;
			cout << "Podaj druga wartosc" << endl;
			cin >> b;
			if (znak == "+")
			{
				cout << a + b << endl;
				break;
			}
			if (znak == "-")
			{

				cout << a - b << endl;
				break;
			}
			if (znak == "*")
			{
				cout << a * b << endl;
				break;
			}
			if (znak == "/")
			{
				cout << a / b << endl;
				break;
			}
		}
		}
		system("pause");
		return 0;
	}
}





